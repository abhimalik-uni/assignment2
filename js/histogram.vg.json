{
  "$schema": "https://vega.github.io/schema/vega-lite/v5.json",
  "data": {
    "url": "https://raw.githubusercontent.com/abhimalik-uni/assignment2/main/data/melb_data.csv"
  },
  "title":"TITLE GOES HERE",
  "transform": [
    {
      "calculate": "datum.Type === 'br' ? 'Bedroom(s)' : (datum.Type === 'h' ? 'House' : (datum.Type === 'u' ? 'Unit' : (datum.Type === 't' ? 'Townhouse' : (datum.Type === 'dev site' ? 'Development Site' : 'Other Residential'))))",
      "as": "Type"
    },
    {"filter": {"selection": "propertyTypeSelect"}}
  ],
  "mark": "bar",
  "selection": {
    "propertyTypeSelect": {
      "type": "single",
      "fields": ["Type"],
      "bind": {
        "input": "select",
        "options": [
          null,
          "House",
          "Unit",
          "Townhouse"
        ],
        "labels": [
          "All types",
          "House",
          "Unit",
          "Townhouse"
        ],
        "name": "Select a property type: "
      }
    }
  },
  "encoding": {
    "x": {
      "bin": true,
      "field": "Distance",
      "type": "quantitative",
      "axis": {"title": "Distance from CBD (km)"}
    },
    "y": {
      "aggregate": "mean",
      "field": "Price",
      "type": "quantitative",
      "axis": {"title": "Average Property Price (AUD)"}
    },
    "color": {
      "field": "Type",
      "type": "nominal",
      "legend": {"title": "Property Type"}
    }
  }
}
